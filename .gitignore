# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# OS
.DS_Store

# Secrets / local env
.env
.envrc

# Claude Code per-user local settings (project-shared settings.json is OK to commit)
.claude/settings.local.json

# Runtime outputs (per-paper artifacts, uploads, build)
/runs
runs/_baseline_recheck.yaml
upload/
upload.zip
build/

# Sibling project (mypaper is its own repo, not part of paper2md)
mypaper/

# Source / input data (PDFs, references, private templates)
参考文献/
*.pdf
*.docx
.worktrees/
# Public outline-template examples ARE committed; they're a load-bearing input
!templates/*.docx

.claude/
.coverage

# v1.12 phase1 eval harness outputs
tests/eval/_ragas_out/
vendor/*.jar
