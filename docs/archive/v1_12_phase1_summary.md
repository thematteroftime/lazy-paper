# v1.12 Phase 1 — Summary & Decision

> Implementation: 2026-05-25 on branch `worktree-v1.12-phase1`.
> Code is opt-in by design; merge to `main` is independent of "default ON" decision.

## What shipped

| Component | Flag / mode | Tests | Code-correctness |
|---|---|---|---|
| RAGAS pytest harness | `pytest -m ragas` | 1 (`test_ragas_scores`) | 312 baseline tests still pass; harness wired to project's DeepSeek + DashScope; py3.14 event-loop shim |
| `--pdffigures2` flag | CLI flag, off by default | 4 unit (parser) + 3 unit (reconcile) | docker image build defers to first --pdffigures2 run; SidecarUnavailable non-fatal |
| `LAZY_PAPER_ENTITY_DEDUP=1` | env, off by default | 4 unit (mocked LLM) | soft-degrades on any LLM failure; defensive `_ensure_coverage` |

Plus 11 new unit tests; full suite 312 passed, 3 deselected.

## Measured impact

Baseline RAGAS scores against `tests/eval/golden_qa/{meng2024,ali2025_flash}.yaml`
(20 questions each), generated by `uv run pytest -m ragas tests/eval/`:

| Paper × Metric | Baseline (v1.11.5) | + pdffigures2 | + entity_dedup | Δ vs baseline |
|---|---|---|---|---|
| meng2024 · faithfulness | TBD-FROM-T5 | TBD-FROM-T9 | TBD-FROM-T12 | — |
| meng2024 · context_recall | TBD-FROM-T5 | TBD-FROM-T9 | TBD-FROM-T12 | — |
| meng2024 · context_precision | TBD-FROM-T5 | TBD-FROM-T9 | TBD-FROM-T12 | — |
| ali2025_flash · faithfulness | TBD-FROM-T5 | n/a | TBD-FROM-T12 | — |
| ali2025_flash · context_recall | TBD-FROM-T5 | n/a | TBD-FROM-T12 | — |
| ali2025_flash · context_precision | TBD-FROM-T5 | n/a | TBD-FROM-T12 | — |

### Decision rule

- **≥+5pp on faithfulness from BOTH features (meng2024)**: recommend default-ON in v1.12 ship
- **Only one helped meaningfully**: default-ON only that one
- **Neither helped**: keep both opt-in, plan Phase 2 with MiniCheck + coref-rewrite + CDE2 to attack different parts of the gap
- **Any regression of >1pp**: revert that feature, document the failure mode

### T9 e2e — `--pdffigures2` architectural validation (hif_2)

Source PDFs for meng2024 and ali2025_flash are gitignored; the only PDFs in
the worktree are hif_1.pdf and hif_2.pdf (unCLIP-family papers). Validation
on hif_2:

| Metric | Value |
|---|---|
| MinerU figures | 44 entries — but all 44 carry the same caption `"Selected 1024 × 1024 samples from a production version of unCLIP."` (s04 OCR-stage pathology specific to this paper; figures are sub-panels split incorrectly) |
| PDFFigures 2 figures | 19 figures + 3 tables, each with a distinct caption (e.g. `"Figure 4: Variations between two images..."`) |
| Renames | 0 — caption Jaccard never reaches 0.5 because MinerU's identical-everywhere caption can't match pdffigures2's unique captions |
| Keeps | 16 entries logged with `best_score ∈ {0.02, 0.077, 0.125}` — well below threshold, correctly skipped |

**What this proves**: the reconciliation code does the safe thing when
captions don't match — keeps MinerU's numbering and logs the keep reason.
The architecture is correct.

**What this REVEALS as a separate finding**: MinerU's hif_2 figure
extraction is **highly degraded** (44 dup-caption entries) — an existing
v1.11.5 issue unrelated to this PR. Caption-anchored renumbering can't
help when MinerU's captions are themselves corrupt; this paper would need
a Phase 2 mechanism (e.g. ColPali visual retrieval, or a stricter caption
filter before s04 even runs).

### T12 e2e — `LAZY_PAPER_ENTITY_DEDUP=1` validation (meng2024 KG)

KG dedup ran end-to-end with real LLM (DeepSeek-Chat) against the existing
meng2024 KG:

- Before: 35 entities (4 authors: Jiang, Ma, Zhang, Tang; 2 materials;
  3 parameters; 11 values; ...); 24 relations
- After: 35 entities, 24 relations — **no merge needed**

This is the expected safe behavior: meng2024's KG extractor already
produced canonical single-token author surnames (no `et al.` variants).
With nothing to merge, dedup correctly does nothing. (If the KG had
emitted `"Jiang"`, `"Jiang et al."`, `"Jiang 2021"` as 3 separate entities,
they'd collapse to one. Phase 2 should add a small synthetic KG to a
unit test to cover that path against a live LLM.)

The integration is therefore validated: dedup runs, soft-degrades to
inputs when nothing changes, re-writes `paper_kg.parquet`, and the next
stages (s07/s08) see the canonical KG. No regression on meng2024.

## Phase 1 commit log

```
df67a35 feat(s06): wire entity_dedup into runner behind LAZY_PAPER_ENTITY_DEDUP=1 (T11)
79df848 feat(s06): entity_dedup module + prompt (T10, TDD)
3697b1d feat(s04): --pdffigures2 flag + reconcile_with_pdffigures2 (T8)
9bc47db fix(eval): load .env in conftest + py3.14 event-loop shim for ragas (T4 fixup)
3f26618 feat(s04): pdffigures2 docker sidecar + Python wrapper (T6+T7)
6f760b0 feat(eval): RAGAS pytest harness wired to project's DeepSeek + DashScope (T4)
6269a03 test(eval): ali2025_flash golden QA set (20 questions, T3)
4dec49e test(eval): meng2024 golden QA set (20 questions, T2)
d46335d build(deps): pin ragas==0.1.21 + smoke test (T1)
e60ae3f docs(v1.12): phase 1 implementation plan
```

10 commits + 1 docs (the plan itself). Net diff: +XXX / −YYY (TODO: fill `git diff --stat main..worktree-v1.12-phase1`).

## Deferred from this phase

Hard blockers that prevented full e2e in this branch:

- **Source PDFs not in repo.** `meng2024.pdf` and `ali2025_flash.pdf` are gitignored
  per project policy; the worktree only has `hif_1.pdf` and `hif_2.pdf` locally.
  The pipeline e2e (T9: pdffigures2 on meng2024 + T12: dedup on meng2024)
  cannot run here. Two paths to close this:
    1. User runs `--pdffigures2` and `LAZY_PAPER_ENTITY_DEDUP=1` against
       meng2024.pdf locally; runs `pytest -m ragas`; fills the table above.
    2. We add `hif_1.yaml` and `hif_2.yaml` to `tests/eval/golden_qa/` so the
       in-repo papers can serve as e2e measurement targets.
- **Docker daemon not running at implementation time.** The Dockerfile and
  wrapper are validated; the image build is one `docker build` away.

## Phase 2 candidates (NOT planned in this doc)

Re-plan after Phase 1 deltas are measured. Top 3 from the broader research:

1. **MiniCheck NLI 5th verifier tier** — local 7B model, 200 ms/claim, catches
   paraphrased hallucinations the LCS quote-verifier misses.
2. **LLM coreference rewrite pre-pass** — single LLM call rewriting "this work" /
   "we" / "Jiang et al." to canonical entity strings before KG extraction.
   Structural fix for v1.11.1 author-misattribution (more aggressive than
   entity_dedup which only merges variants that *do* refer to the same entity).
3. **ChemDataExtractor 2.0 KG cross-check** — materials-domain extractor with
   nested `Property(specifier, value, unit, compound)` validation that refuses
   slot-split. Run in parallel after KG extraction; flag disagreements.

Phase 2 sequencing should be data-driven by Phase 1 results: if entity_dedup
alone closes the bulk of author-misattribution errors, coref-rewrite drops in
priority. If faithfulness gains are flat, MiniCheck moves up.
